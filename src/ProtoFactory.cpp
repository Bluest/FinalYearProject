#include "ProtoFactory.h"

void ProtoFactory::draw(SDL_Renderer* _renderer)
{
	// Draw a square with a large circle inside it
}

void ProtoFactory::onLeftClick()
{
	// Create unit at a random point outside the factory
}
